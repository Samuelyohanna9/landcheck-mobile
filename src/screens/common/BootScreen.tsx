import { LoadingView } from "../../components/LoadingView";

export const BootScreen = () => (
  <LoadingView
    title="Preparing LandCheck Green"
    subtitle="Restoring session, checking local storage, and loading the native Green workspace."
  />
);
